# История работ — API BM (Ozon)

## Что сделано

### Структура проекта
- `common/sheets.py` — общий модуль записи в Google Sheets
- `ozon/orders_fbs.py` — обновление заказов FBS + FBO каждые 30 мин
- `ozon/load_history.py` — историческая загрузка за указанный период (вручную)
- `requirements.txt` — зависимости: requests, gspread, google-auth

### Google Таблица
- ID: `1q7nDWrMge3XwlplH5LOBa0z6aryp7PcIkBIjclzorQ4`
- Лист **API - Заказы FBS** — заказы FBS
- Лист **API - Заказы FBO** — заказы FBO
- Старый лист **Заказы BM** — удалён

### Столбцы (оба листа)
| # | Название |
|---|----------|
| A | Служебная ячейка (Обновлен: / дата / время МСК) |
| B | Номер заказа |
| C | Номер отправления |
| D | Принят в обработку |
| E | Дата отгрузки |
| F | Статус |
| G | Артикул |
| H | Цена продажи |
| I | Количество |
| J | Кластер отгрузки |
| K | Кластер доставки |
| L | Оплатили |
| M | СПП (десятичная дробь с запятой) |

### Логика обновления (`common/sheets.py`)
- Читает существующие данные листа
- Мержит по `Номер отправления` (обновляет статусы, добавляет новые)
- Сортирует по дате по возрастанию (свежие снизу)
- Записывает обратно через `worksheet.resize()` + `worksheet.update()`
- Время обновления — UTC+3 (Москва)

### GitHub Actions
| Воркфлоу | Расписание | Скрипт |
|----------|-----------|--------|
| Ozon BM — Заказы FBS и FBO | каждые 30 мин | `ozon/orders_fbs.py` |
| Ozon BM — История заказов | вручную | `ozon/load_history.py` |
| Ozon — Debug | вручную | `ozon/debug_order.py` |

### Секреты GitHub
- `OZON_BM_CLIENT_ID`
- `OZON_BM_API_KEY`
- `GOOGLE_CREDENTIALS` — JSON сервисного аккаунта Google

### Источники данных Ozon API
- **FBS**: `POST /v3/posting/fbs/list` с `financial_data: true`
- **FBO кластеры**: `POST /v2/posting/fbo/list` с `financial_data: true`
- **FBO цены**: `POST /v1/report/postings/create` → CSV отчёт (поле "Оплачено покупателем")

### Историческая загрузка
Данные загружены за периоды:
- 2025-06-01 → 2025-12-01
- 2025-12-01 → 2026-03-01
- 2026-03-01 → 2026-06-01

Для загрузки следующего периода: изменить `DATE_FROM` / `DATE_TO` в `ozon/load_history.py` и запустить воркфлоу вручную.

### Известные проблемы и решения
- **Лимит 10М ячеек Google Sheets**: решено через `worksheet.resize()` перед записью вместо `worksheet.clear()`
- **FBO v3 возвращает 0 заказов**: используем v2 endpoint
- **`customer_price` нет в v2 API**: берём из CSV отчёта
- **Потеря данных при ошибке**: `clear()` заменён на `resize()` — при падении `update()` данные не стираются
