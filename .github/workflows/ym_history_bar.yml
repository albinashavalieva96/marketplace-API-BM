name: ЯМ Бар — История заказов (только вручную)

on:
  workflow_dispatch:

jobs:
  load-ym-bar-history:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - name: Run ЯМ Бар history script
        env:
          YM_BAR_API_TOKEN: ${{ secrets.YM_BAR_API_TOKEN }}
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: python ym/load_history_bar.py
